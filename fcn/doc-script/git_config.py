# Usando o git

    $ git config --global user.email "nome@gmail.com"
    $ git config --global user.name "Seu nome"

    $ git config --global core.editor vim
    $ git config --global push.default simple

# Inicializa git repo:

    $ git init .
    $ git add --all .

# Efetua o commit:

    $ git commit -am "Inicio do commit programa"